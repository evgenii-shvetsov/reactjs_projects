import React from "react";

const Header = () => {
/*
const [defaultMode, setMode] = useState(true);
const modeColor = defaultMode ? 'light' : 'dark';  */
  return (
    <header className = "header">
      <h1>AS Degree in CNIT Department</h1>
      <p>Add or delete a course using the input box below</p>
    </header>
  )
}

export default Header